using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PuzzleBox : MonoBehaviour
{
    /*private GraphicComponentsList ifList = new ListIf();
    private GraphicComponentsList messageList = new ListMessage();
    private GraphicComponentsList procList = new ListProc();
    private GraphicComponentsList condList = new ListCondition();
    private GraphicComponentsList actTList = new ListActionT();
    private GraphicComponentsList actNTList = new ListActionNT();*/

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
